name: Inspect Swolvy htmlhost Payload 1249
on:
  push:
    branches: [main]
jobs:
  inspect:
    if: github.event.head_commit.message == 'Run Swolvy htmlhost payload 1249'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/inspect_swolvy_htmlhost_payload_1249.py
