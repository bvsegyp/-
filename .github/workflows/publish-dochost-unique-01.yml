name: Publish Unique DocHost Backlink
on:
  push:
    branches: [main]
jobs:
  publish:
    if: github.event.head_commit.message == 'Run unique DocHost backlink'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/inspect_publish_dochost_unique.py | tee publish.log
      - run: grep '^RESULT_URL=https://' publish.log || true
